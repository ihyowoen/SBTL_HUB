name: Patch review 4840783305

on:
  push:
    branches:
      - agent/structural-news-value-v3
    paths:
      - "scripts/patch_review_4840783305.py"
      - ".github/workflows/patch-review-4840783305.yml"

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    env:
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/structural-news-value-v3
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply review fixes
        run: python -B scripts/patch_review_4840783305.py

      - name: Compile validators without bytecode
        run: |
          python -B - <<'PY'
          from pathlib import Path
          for path in Path('validation_scripts').rglob('*.py'):
              compile(path.read_text(encoding='utf-8'), str(path), 'exec')
          PY

      - name: Run focused review tests
        run: python -B -m unittest validation_scripts.tests.test_review_4840783305_contracts -v

      - name: Run full regression suite
        run: python -B -m unittest discover -s validation_scripts/tests -v

      - name: Verify prompt overlays
        run: python -B validation_scripts/apply_prompt_contract_overlays.py --check

      - name: Remove temporary patch files
        run: |
          rm -f scripts/patch_review_4840783305.py
          rm -f .github/workflows/patch-review-4840783305.yml
          find validation_scripts -type d -name __pycache__ -prune -exec rm -rf {} +
          find . -name '*.pyc' -delete

      - name: Commit verified fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: address review 4840783305 contracts"
          git push origin HEAD:agent/structural-news-value-v3
