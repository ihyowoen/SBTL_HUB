name: Workflow contract validation

on:
  push:
    branches:
      - agent/stage-a-0-7c-six-validator
    paths:
      - "validation_scripts/tests/test_stage_a_a034_targeted_rereview_r5_fixture.py"
      - "validation_scripts/tests/fixtures/stage_a_a034_r4_exact*"
      - ".github/workflows/workflow-contract-validation.yml"

permissions:
  contents: write

jobs:
  validate-a034-stage-a-r5:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Compile current-main-derived validators
        run: python -m compileall -q validation_scripts

      - name: Validate exact A034 Stage A R5 fixture
        id: a034_r5_fixture
        shell: bash
        run: |
          mkdir -p validation_results
          set +e
          python -m unittest validation_scripts.tests.test_stage_a_a034_targeted_rereview_r5_fixture -v > validation_results/stage_a_a034_targeted_rereview_r5_validator_log.txt 2>&1
          code=$?
          cat validation_results/stage_a_a034_targeted_rereview_r5_validator_log.txt
          exit $code

      - name: Persist A034 Stage A R5 receipt
        if: always()
        env:
          FIXTURE_OUTCOME: ${{ steps.a034_r5_fixture.outcome }}
        run: |
          mkdir -p validation_results
          {
            echo "workflow=Workflow contract validation"
            echo "head_sha=${GITHUB_SHA}"
            echo "branch=${GITHUB_REF_NAME}"
            echo "fixture_outcome=${FIXTURE_OUTCOME}"
            echo "artifact_sha256=dc61c625c3e6e1d736da0b5cf3b428b5593876a62edef3c81c844a31a5c88db8"
            echo "artifact_bytes=41655"
            echo "source_r4_sha256=7c57e832a34bb1284893cd843445833ab16beb4bdf092c6006d5b7334e2e46e8"
            echo "prompt_0_1_git_blob_sha=57719c5eae5e219d704baad708992df79f8721bb"
            echo "prompt_0_1_sha256=0795fb16b89dc320a26e5ecb05965ffc67b73e06137e10a5c2a8132c01a846bc"
            if [ "${FIXTURE_OUTCOME}" = "success" ]; then
              echo "stage_a_a034_targeted_rereview_r5_validator=PASS"
            else
              echo "stage_a_a034_targeted_rereview_r5_validator=BLOCKED"
            fi
          } > validation_results/stage_a_a034_targeted_rereview_r5_validator_result.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- validation_results/stage_a_a034_targeted_rereview_r5_validator_result.txt validation_results/stage_a_a034_targeted_rereview_r5_validator_log.txt
          git commit -m "ci: persist A034 Stage A R5 validator diagnostics" || exit 0
          git push origin "HEAD:${GITHUB_REF_NAME}"
