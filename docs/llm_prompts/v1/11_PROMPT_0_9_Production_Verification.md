# Prompt 0.9 — Production Verification V4

**Status:** `ACTIVE_CANONICAL`  
**Version:** `PROMPT_0_9_V4_20260829`

After merge verify new main/merge commit, canonical count/operations, lean consistency, GitHub Actions/deployment, production endpoint data, introduced/updated IDs, card rendering, live Related target existence, no provisional relation IDs, Related navigation when available, and HTML/interactive/mobile surfaces separately when testable.

Allowed overall status: `PASS`, `PASS_WITH_LIMITATIONS`, `FAIL`. Record data/deployment/HTML/interactive/mobile verification separately. Never mark untested mandatory surfaces true. `production_verified = true` only when the active contract's mandatory surfaces are actually verified.