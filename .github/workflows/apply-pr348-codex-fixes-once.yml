name: apply-pr348-codex-fixes-once

on:
  push:
    branches:
      - chatgpt/monthly-brief-domain-cleanup-20260908
    paths:
      - "scripts/apply_pr348_codex_fixes.mjs"
      - ".github/workflows/apply-pr348-codex-fixes-once.yml"

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[pr348-codex-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/monthly-brief-domain-cleanup-20260908
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - name: Apply fixes
        run: node scripts/apply_pr348_codex_fixes.mjs
      - name: Diff integrity
        run: git diff --check
      - name: Targeted boundary tests
        run: npx vitest run __tests__/monthlyBriefProductBoundary.test.js __tests__/monthlyBriefPublication.test.js __tests__/monthlyBriefProvenance.test.js __tests__/quickAnalysisLabels.test.js
      - name: Validate official Monthly Brief library
        run: npm run validate:monthly-briefs
      - name: Full unit tests
        run: npm test
      - name: Production build
        run: npm run build
      - name: Commit validated fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/chat/appCommand.js src/kang.js src/MonthlyBriefPanel.jsx
          git commit -m "Address PR348 Codex review findings [pr348-codex-applied]"
          git push origin HEAD:chatgpt/monthly-brief-domain-cleanup-20260908
