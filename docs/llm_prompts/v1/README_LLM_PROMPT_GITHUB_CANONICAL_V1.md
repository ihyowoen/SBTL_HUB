# SBTL_HUB LLM Prompt Package V4

**Stable path:** `docs/llm_prompts/v1/`  
**Active version:** `LLM_PROMPT_CANONICAL_V4_20260829`

The directory name remains `v1` for path stability; manifest version defines the live contract.

Ordinary new-news runs start with `00_NEW_RUN_MASTER_PROMPT.md`, then current-main lock and 0.0D.

Active named prompts are complete operating contracts. No active prompt is assembled from later overrides, hardening addenda, overlays, or subordinate legacy bodies.

Pipeline: `0.0D → Input Audit → 0.0C → event-universe lock/reconciliation → A → B → C → revise → 0.4 Addability → 0.5 → 0.6 → 0.7 → 0.7C → 0.8 → merge → 0.9 → optional 1.0/1.1`.

Stage A embeds item news value and Related pre-pass. Portfolio news value/completeness lives in the Editorial standard. Related lineage locks at Stage C; 0.4 revalidates addability against current baseline.

`data/cards.full.json` is canonical; `public/data/cards.json` is generated lean projection. Formal ordinary operations are `insert`, `update`, `related_add`. A separate governed manual-direct-add mode exists for already-reviewed bounded changes and does not impersonate formal stage completion.

Files marked `SUPERSEDED`, `REFERENCE_ONLY`, archived, or completed migration are audit material only. 0.0D inventories them but does not apply them as ordinary-run authority.