# PR #233 Review 4842822807 Validation

## Findings closed

1. Final `related[]` and provisional `related_candidate_spec_ids[]` are deduplicated against one shared resolved-target identity space. Distinct aliases resolving to the same card now fail before Prompt 0.8 can materialize a duplicate final edge.
2. Stage A generic evidence-target matching now applies Unicode NFKC normalization and removes Unicode punctuation, symbols, paired quotes, and bracket wrappers from both edges before placeholder matching.

## Regression coverage

`validation_scripts/tests/test_review_4842822807.py` covers:

- final/provisional aliases resolving to the same target;
- CJK terminal punctuation;
- Unicode ellipsis;
- curly quotation marks;
- CJK and full-width bracket wrappers;
- preservation of a concrete target carrying Unicode terminal punctuation.

## Validation

- Python compile: PASS
- focused review tests: PASS
- existing workflow-contract tests: PASS
- prompt contract overlay check: PASS
- generated `__pycache__/*.pyc`: absent from final diff
- temporary patch workflows/helpers: absent from final diff
- card data and production IDs: unchanged
