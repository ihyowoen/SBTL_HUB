# Prompt 0.3 — Stage C Fact-Safe Red Team & Lineage Lock V4

**Status:** `ACTIVE_CANONICAL`  
**Version:** `STAGE_C_V4_20260829`

Independently validate Stage B drafts/evidence. Stage C creates `accepted_fact_safe`, not addable or publish-ready state.

Challenge source direction/independence, claim-source and quote alignment, all numbers/dates/entities/counterparties, event stage/date role, stale republication/same-event duplication, selected news-value route/anchor evidence, policy/earnings/technology caveats, unsupported causality/strategic overreach, visible-field consistency, full-schema viability, and Related evidence.

Accepted items must have exactly one source-supported active route: `execution_anchor_route` or `structural_non_execution_route`. Carry the Stage A before/after chain and anchor classes. Machine-compatibility metadata may be emitted where required by existing validators.

Every accepted new card emits `related_lineage.status = PASS` and a final relation type allowed by the Related contract. Do not accept as a new card `same_event_duplicate`, `existing_card_reinforcement`, or `uncertain_needs_review`. A distinct follow-up requires direct predecessor, fresh anchor class/anchor, incremental fact, changed judgment, stage/date precision, and reason reinforcement alone is insufficient.

Outcomes: `accepted_fact_safe`, `revise_required`, `rejected`, `support_source_only`, `deferred_review_pool`. Every outcome is accounted and preserves explicit defect/rescue state.

Accepted output preserves evidence/source/date/selection package and emits locked `related_lineage`, route validation, visible-field fact-safe result, unresolved downstream issues, and prompt provenance. Prompt 0.4 still must revalidate against latest canonical.