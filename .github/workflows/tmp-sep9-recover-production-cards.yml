name: tmp-sep9-recover-production-cards
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  closed:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'CLOSED: only 2 of intended 4 compressed payload parts were committed; gzip EOF confirmed. Exact-source Stage A recovery is authoritative.'
